/* app/page.module.css — TheWall Design System */

/* ── LOGIN ──────────────────────────────────────────────────────── */
.loginWrap {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 24px;
  position: relative;
}

.scanline {
  position: fixed;
  top: -5%;
  left: 0;
  right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent, rgba(0,179,247,0.15), transparent);
  animation: scanline 8s linear infinite;
  pointer-events: none;
  z-index: 1;
}

.loginCard {
  width: 100%;
  max-width: 420px;
  background: rgba(7, 13, 20, 0.95);
  border: 1px solid var(--border-bright);
  border-radius: 16px;
  padding: 40px 36px;
  position: relative;
  z-index: 2;
  box-shadow:
    0 0 60px rgba(0,179,247,0.08),
    0 0 0 1px rgba(0,179,247,0.05),
    inset 0 1px 0 rgba(0,179,247,0.1);
}

.logo {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 32px;
}

.hexLogo {
  font-size: 3rem;
  color: var(--cyan);
  filter: drop-shadow(0 0 20px rgba(0,179,247,0.6));
  animation: float 3s ease-in-out infinite;
}

.logoTitle {
  font-family: var(--font-display);
  font-size: 1.6rem;
  font-weight: 800;
  color: var(--text);
  letter-spacing: 0.15em;
}

.logoSub {
  font-size: 0.65rem;
  color: var(--text-muted);
  letter-spacing: 0.1em;
  margin-top: 2px;
}

.loginDesc {
  font-size: 0.8rem;
  color: var(--text-dim);
  line-height: 1.7;
  margin-bottom: 24px;
  text-align: center;
}

.featureRow {
  display: flex;
  gap: 8px;
  margin-bottom: 24px;
  justify-content: center;
}

.featureChip {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: rgba(0,179,247,0.06);
  border: 1px solid var(--border);
  border-radius: 20px;
  font-size: 0.65rem;
  color: var(--cyan-dim);
  white-space: nowrap;
}

.btnPrimary {
  width: 100%;
  padding: 14px;
  background: linear-gradient(135deg, rgba(0,179,247,0.2), rgba(0,179,247,0.08));
  border: 1px solid var(--border-bright);
  border-radius: var(--radius);
  color: var(--cyan);
  font-family: var(--font-mono);
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  cursor: pointer;
  transition: all 0.2s;
  margin-bottom: 12px;
}

.btnPrimary:hover {
  background: rgba(0,179,247,0.25);
  border-color: var(--cyan);
  box-shadow: 0 0 20px rgba(0,179,247,0.2);
  transform: translateY(-1px);
}

.btnSecondary {
  width: 100%;
  padding: 12px;
  background: transparent;
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: var(--radius);
  color: var(--text-dim);
  font-family: var(--font-mono);
  font-size: 0.78rem;
  cursor: pointer;
  transition: all 0.2s;
  margin-bottom: 20px;
}

.btnSecondary:hover {
  border-color: rgba(255,255,255,0.2);
  color: var(--text);
}

.btnGhost {
  width: 100%;
  padding: 10px;
  background: transparent;
  border: none;
  color: var(--text-muted);
  font-family: var(--font-mono);
  font-size: 0.75rem;
  cursor: pointer;
  margin-top: 8px;
  transition: color 0.2s;
}

.btnGhost:hover { color: var(--text-dim); }

.gasNote {
  text-align: center;
  font-size: 0.62rem;
  color: rgba(0,255,136,0.5);
  letter-spacing: 0.05em;
  margin-top: 4px;
}

.input {
  width: 100%;
  padding: 13px 16px;
  background: rgba(0,0,0,0.4);
  border: 1px solid var(--border-bright);
  border-radius: var(--radius);
  color: var(--text);
  font-family: var(--font-mono);
  font-size: 0.85rem;
  margin-bottom: 16px;
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
}

.input:focus {
  border-color: var(--cyan);
  box-shadow: 0 0 0 3px rgba(0,179,247,0.1);
}

.input::placeholder { color: var(--text-muted); }

.creating {
  text-align: center;
  padding: 20px 0;
}

.spinner {
  width: 40px;
  height: 40px;
  border: 2px solid rgba(0,179,247,0.15);
  border-top-color: var(--cyan);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  margin: 0 auto 16px;
}

.creatingNote {
  font-size: 0.7rem;
  color: var(--text-muted);
  margin-top: 8px;
}

.loginFooter {
  margin-top: 32px;
  font-size: 0.6rem;
  color: var(--text-muted);
  letter-spacing: 0.15em;
  text-align: center;
  position: relative;
  z-index: 2;
}

/* ── DASHBOARD ──────────────────────────────────────────────────── */
.dashWrap {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  max-width: 900px;
  margin: 0 auto;
  padding: 0 16px;
  position: relative;
  z-index: 1;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 0 16px;
  border-bottom: 1px solid var(--border);
  position: sticky;
  top: 0;
  background: rgba(3,5,8,0.95);
  backdrop-filter: blur(12px);
  z-index: 10;
}

.headerLeft {
  display: flex;
  align-items: center;
  gap: 10px;
}

.hexSmall {
  color: var(--cyan);
  font-size: 1.4rem;
  filter: drop-shadow(0 0 8px rgba(0,179,247,0.5));
}

.headerTitle {
  font-family: var(--font-display);
  font-size: 1.1rem;
  font-weight: 800;
  letter-spacing: 0.2em;
  color: var(--text);
}

.headerRight {
  display: flex;
  align-items: center;
  gap: 8px;
}

.chainBadge {
  padding: 3px 8px;
  background: rgba(0,179,247,0.06);
  border: 1px solid var(--border);
  border-radius: 4px;
  font-size: 0.6rem;
  color: var(--cyan-dim);
  letter-spacing: 0.1em;
}

.refreshBtn, .logoutBtn {
  padding: 6px 10px;
  background: transparent;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  color: var(--text-dim);
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.2s;
}

.refreshBtn:hover, .logoutBtn:hover {
  border-color: var(--cyan-dim);
  color: var(--cyan);
}

.main {
  flex: 1;
  padding: 24px 0;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* ── Wallet Card ── */
.walletCard {
  background: linear-gradient(135deg, rgba(0,179,247,0.06), rgba(0,0,0,0.3));
  border: 1px solid var(--border-bright);
  border-radius: 12px;
  padding: 24px;
  position: relative;
  overflow: hidden;
}

.walletCard::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -20%;
  width: 200px;
  height: 200px;
  background: radial-gradient(circle, rgba(0,179,247,0.05), transparent 70%);
  pointer-events: none;
}

.walletTop {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  flex-wrap: wrap;
  gap: 16px;
  margin-bottom: 20px;
}

.walletLabel {
  font-size: 0.6rem;
  color: var(--cyan-dim);
  letter-spacing: 0.15em;
  margin-bottom: 6px;
}

.walletAddr {
  font-size: 0.9rem;
  color: var(--text);
  display: flex;
  align-items: center;
  gap: 8px;
}

.walletEmail {
  font-size: 0.7rem;
  color: var(--text-muted);
  margin-top: 4px;
}

.copyBtn {
  background: transparent;
  border: none;
  cursor: pointer;
  font-size: 0.9rem;
  opacity: 0.6;
  transition: opacity 0.2s;
  padding: 0;
}

.copyBtn:hover { opacity: 1; }

.walletTotal { text-align: right; }

.totalLabel {
  font-size: 0.6rem;
  color: var(--text-muted);
  letter-spacing: 0.12em;
  margin-bottom: 4px;
}

.totalAmount {
  font-family: var(--font-display);
  font-size: 2rem;
  font-weight: 800;
  color: var(--text);
  background: linear-gradient(135deg, #fff, var(--cyan));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.loading {
  color: var(--text-muted);
  animation: blink 1.2s ease-in-out infinite;
  -webkit-text-fill-color: var(--text-muted);
}

.goalSection { margin-top: 4px; }

.goalRow {
  display: flex;
  justify-content: space-between;
  margin-bottom: 8px;
}

.goalLabel {
  font-size: 0.65rem;
  color: var(--text-muted);
  letter-spacing: 0.08em;
}

.goalPct {
  font-size: 0.65rem;
  color: var(--cyan-dim);
  font-weight: 700;
}

.goalBar {
  height: 3px;
  background: rgba(255,255,255,0.05);
  border-radius: 2px;
  overflow: hidden;
}

.goalFill {
  height: 100%;
  background: linear-gradient(90deg, var(--cyan), var(--gold));
  border-radius: 2px;
  transition: width 1s ease;
  box-shadow: 0 0 8px rgba(0,179,247,0.4);
  min-width: 3px;
}

/* ── Emocoin ── */
.emoSection { }

.emoCard {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 16px 20px;
  background: rgba(255,215,0,0.04);
  border: 1px solid rgba(255,215,0,0.15);
  border-radius: 10px;
}

.emoIcon { font-size: 1.5rem; }

.emoTitle {
  font-size: 0.6rem;
  color: var(--gold-dim);
  letter-spacing: 0.15em;
  margin-bottom: 2px;
}

.emoBalance {
  font-family: var(--font-display);
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--gold);
}

.emoRight { margin-left: auto; text-align: right; }

.emoPrice {
  font-size: 0.65rem;
  color: var(--text-muted);
  margin-bottom: 6px;
}

.claimBtn {
  padding: 5px 12px;
  background: rgba(255,215,0,0.1);
  border: 1px solid rgba(255,215,0,0.25);
  border-radius: 20px;
  color: var(--gold);
  font-family: var(--font-mono);
  font-size: 0.65rem;
  cursor: pointer;
  transition: all 0.2s;
}

.claimBtn:hover {
  background: rgba(255,215,0,0.2);
  transform: translateY(-1px);
}

/* ── Tabs ── */
.tabs {
  display: flex;
  gap: 4px;
  border-bottom: 1px solid var(--border);
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.tab {
  padding: 10px 18px;
  background: transparent;
  border: none;
  border-bottom: 2px solid transparent;
  color: var(--text-muted);
  font-family: var(--font-mono);
  font-size: 0.72rem;
  letter-spacing: 0.08em;
  cursor: pointer;
  transition: all 0.2s;
  white-space: nowrap;
  text-transform: uppercase;
  margin-bottom: -1px;
}

.tab:hover { color: var(--text-dim); }

.tabActive {
  color: var(--cyan) !important;
  border-bottom-color: var(--cyan) !important;
}

/* ── Token Grid ── */
.tabContent { min-height: 200px; }

.tokenGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 10px;
}

.tokenCard {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px;
  background: rgba(255,255,255,0.02);
  border: 1px solid var(--border);
  border-radius: 10px;
  transition: all 0.2s;
  cursor: default;
}

.tokenCard:hover {
  border-color: var(--border-bright);
  background: rgba(0,179,247,0.03);
  transform: translateY(-1px);
}

.tokenLeft {
  display: flex;
  align-items: center;
  gap: 12px;
}

.tokenDot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  flex-shrink: 0;
  box-shadow: 0 0 6px currentColor;
}

.tokenDotSmall {
  display: inline-block;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  margin-right: 8px;
}

.tokenSymbol {
  font-family: var(--font-display);
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--text);
}

.tokenName {
  font-size: 0.65rem;
  color: var(--text-muted);
  margin-top: 1px;
}

.tokenChain {
  font-size: 0.55rem;
  color: rgba(0,179,247,0.4);
  margin-top: 2px;
  letter-spacing: 0.08em;
}

.tokenRight { text-align: right; }

.tokenPrice {
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--text);
  font-family: var(--font-display);
}

.tokenChange {
  font-size: 0.65rem;
  margin-top: 2px;
}

.tokenBal {
  font-size: 0.62rem;
  color: var(--text-muted);
  margin-top: 3px;
}

.tokenLive {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 4px;
  font-size: 0.55rem;
  color: var(--text-muted);
  margin-top: 4px;
}

.liveDot {
  width: 5px;
  height: 5px;
  background: var(--green);
  border-radius: 50%;
  animation: pulse-ring 2s ease-in-out infinite;
  flex-shrink: 0;
}

.green { color: var(--green); }
.red { color: var(--red); }

/* ── Price Table ── */
.errorBanner {
  padding: 10px 16px;
  background: rgba(255,68,102,0.08);
  border: 1px solid rgba(255,68,102,0.2);
  border-radius: 8px;
  font-size: 0.72rem;
  color: #ff8899;
  margin-bottom: 16px;
}

.priceTable { border: 1px solid var(--border); border-radius: 10px; overflow: hidden; }

.priceHeader {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  padding: 10px 16px;
  background: rgba(0,179,247,0.04);
  font-size: 0.6rem;
  color: var(--text-muted);
  letter-spacing: 0.1em;
  text-transform: uppercase;
  border-bottom: 1px solid var(--border);
}

.priceRow {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  padding: 13px 16px;
  font-size: 0.8rem;
  border-bottom: 1px solid var(--border);
  transition: background 0.15s;
  align-items: center;
}

.priceRow:last-child { border-bottom: none; }
.priceRow:hover { background: rgba(0,179,247,0.02); }

.priceVal { font-weight: 700; color: var(--text); }

/* ── History ── */
.historySection { display: flex; flex-direction: column; gap: 10px; }

.historyItem {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  background: rgba(255,255,255,0.02);
  border: 1px solid var(--border);
  border-radius: 10px;
}

.historyLeft { display: flex; align-items: center; gap: 12px; }

.historyIcon {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0,179,247,0.08);
  border-radius: 8px;
  font-size: 1rem;
  color: var(--cyan);
}

.historyTitle { font-size: 0.8rem; color: var(--text); margin-bottom: 3px; }
.historyDate { font-size: 0.62rem; color: var(--text-muted); }

.historyRight { text-align: right; }
.historySig { font-size: 0.7rem; color: var(--cyan-dim); }
.historyFee { font-size: 0.62rem; color: var(--text-muted); margin-top: 2px; }

.historyNote {
  padding: 12px 16px;
  background: rgba(0,179,247,0.03);
  border: 1px dashed var(--border);
  border-radius: 8px;
  font-size: 0.7rem;
  color: var(--text-muted);
  text-align: center;
}

/* ── Treasury ── */
.treasurySection { display: flex; flex-direction: column; gap: 10px; }

.treasuryCard {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px;
  background: rgba(255,255,255,0.02);
  border: 1px solid var(--border);
  border-radius: 10px;
}

.treasuryIcon {
  font-size: 1.4rem;
  width: 40px;
  text-align: center;
  flex-shrink: 0;
}

.treasuryLabel {
  font-size: 0.6rem;
  color: var(--text-muted);
  letter-spacing: 0.12em;
  margin-bottom: 4px;
}

.treasuryAddr {
  font-size: 0.68rem;
  color: var(--text-dim);
  word-break: break-all;
}

.webhookStatus {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 16px;
  background: rgba(0,255,136,0.03);
  border: 1px solid rgba(0,255,136,0.1);
  border-radius: 8px;
  font-size: 0.68rem;
  color: rgba(0,255,136,0.6);
}

/* ── Footer ── */
.footer {
  padding: 20px 0;
  text-align: center;
  font-size: 0.6rem;
  color: var(--text-muted);
  letter-spacing: 0.15em;
  border-top: 1px solid var(--border);
  margin-top: auto;
}

/* ── Responsive ── */
@media (max-width: 600px) {
  .loginCard { padding: 28px 20px; }
  .walletTop { flex-direction: column; }
  .walletTotal { text-align: left; }
  .totalAmount { font-size: 1.6rem; }
  .tokenGrid { grid-template-columns: 1fr; }
  .featureRow { flex-wrap: wrap; }
  .headerTitle { font-size: 0.9rem; }
  .chainBadge { display: none; }
  .priceHeader, .priceRow { grid-template-columns: 1fr 1fr 1fr; font-size: 0.7rem; }
}
